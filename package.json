{
  "name": "spain-consulate-appointments",
  "version": "1.0.0",
  "description": "Basic script that automatically checks for appointments and notifies user if anything is found",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "render-install": "npm install && node node_modules/puppeteer/install.mjs",
    "render-run": "npm install && node node_modules/puppeteer/install.mjs && node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "puppeteer": "^22.12.1"
  }
}
